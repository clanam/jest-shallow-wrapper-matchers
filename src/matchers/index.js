import { toBeOfComponentType } from './to-be-of-component-type';
import { toHaveStyle } from './to-have-style';
import { toHaveText } from './to-have-text';

const matchers = {
  toBeOfComponentType,
  toHaveStyle,
  toHaveText,
};

export { matchers };
